# File này chỉ mang tính THAM KHẢO — Render KHÔNG đọc trực tiếp file .env.
# Khi deploy qua Render Blueprint (render.yaml), bạn sẽ được yêu cầu nhập
# biến này trong Render Dashboard (vì render.yaml đặt "sync: false").
#
# Tạo chuỗi ngẫu nhiên bằng lệnh: openssl rand -hex 32

BROWSER_SERVICE_TOKEN=doi-thanh-mot-chuoi-bi-mat-dai-va-kho-doan
