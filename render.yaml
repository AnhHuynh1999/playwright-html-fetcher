services:
  - type: web
    name: playwright-html-fetcher
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: BROWSER_SERVICE_TOKEN
        sync: false # bắt buộc nhập tay trong dashboard, không lưu trong repo
    healthCheckPath: /health

