services:
  - type: web
    name: playwright-html-fetcher
    runtime: docker
    plan: free
    dockerfilePath: ./browser-service/Dockerfile
    dockerContext: ./browser-service
    envVars:
      - key: BROWSER_SERVICE_TOKEN
        sync: false
      - key: WSJ_COOKIES_JSON
        sync: false
    healthCheckPath: /health